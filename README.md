# 📒 Phone Book CLI (دفترچه تلفن)

A simple **command-line phone book application** written in Python.  
This app allows you to add, search, update, and delete contacts directly from the terminal.  
The interface and messages are in **Persian (Farsi)**.

---

## ✨ Features
- ➕ Add a new contact with a phone number
- 📱 Add additional numbers to an existing contact
- 🔍 Search for a contact by name
- ❌ Delete a contact
- 📊 Show the total number of contacts
- 🚪 Exit the program gracefully

---

## 🛠️ Requirements
- Python **3.10+** (uses `match-case` syntax introduced in Python 3.10)
- Works on **Windows**, **Linux**, and **macOS**

---

## ▶️ How to Run
1. Clone the repository:
   ```bash
   git clone https://github.com/your-username/phone-book-cli.git
   cd phone-book-cli
